{
  "name": "santa-letters-pdf",
  "version": "1.0.0",
  "description": "Santa Letter PDF Generator",
  "main": "server.js",
  "engines": {
    "node": "18.x"
  },
  "scripts": {
  "start": "node server.js",
  "dev": "nodemon server.js",
  "test": "echo \"Error: no test specified\" && exit 1",
  "build": "npm install --production"
},
  "dependencies": {
    "express": "^4.18.2",
    "puppeteer-core": "^21.0.0",
    "@sparticuz/chromium": "^119.0.0",
    "dotenv": "^16.0.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.10",
    "puppeteer": "^21.0.0"
  }
}
